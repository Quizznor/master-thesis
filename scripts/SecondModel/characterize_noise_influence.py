#!/usr/bin/python3

import sys

sys.dont_write_bytecode = True

from binaries import *

# PredictionDataset = DataSetGenerator("second_simulation/tensorflow/signal/", train = False)
# EventClassifier = Classifier()

# traces, labels = PredictionDataset.__getitem__(0)
# trace = np.reshape(traces[0], newshape = (1, len(traces[0])))





# # EventClassifier.predict(PredictionDataset, )
